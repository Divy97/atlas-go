services:
  backend:
    # This assumes you have a Dockerfile that builds your Spring Boot app
    build: .
    image: atlas-go-backend
    ports:
      - "8080:8080"
    # This is the key part! It tells Docker Compose to load the .env file
    env_file:
      - .env